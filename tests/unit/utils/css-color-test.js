import cssColor from 'dummy/utils/css-color';
import { module, test } from 'qunit';

module('Unit | Utility | css color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cssColor();
  assert.ok(result);
});
